<%= form_for(tree) do |f| %>
  <% if tree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tree.errors.count, "error") %> prohibited this tree from being saved:</h2>

      <ul>
      <% tree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">
    <div class="field">
      <%= f.label :title, class: 'label' %>
      <p class="control">
        <%= f.text_field :title %>
      </p>
    </div>

    <div class="field">
      <%= f.label :raw_text, class: 'label' %>
      <p class="control">
        <%= f.text_area :raw_text %>
      </p>
    </div>

    <div class="actions">
      <%= f.submit class: 'button is-primary' %>
    </div>
  </div>
<% end %>
